{
  "name": "seyir-atlasi",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check auth.js && node --check analytics.js && node --check movies.js && node --check series.js && node --check profile.js && node --check navigation.js && node --check public-profile.js && node --check shared-list.js",
    "test": "node --test"
  },
  "dependencies": {
    "nodemailer": "^9.0.3",
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  }
}
